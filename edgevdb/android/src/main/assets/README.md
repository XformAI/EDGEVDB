# Android Assets

Place the following files here for bundling with the APK:

- `model.onnx` — all-MiniLM-L6-v2 ONNX model (quantized recommended)
- `vocab.txt` — WordPiece vocabulary (30,522 tokens)

See `../../models/README.md` for download instructions.
